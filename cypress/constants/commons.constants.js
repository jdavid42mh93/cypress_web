export const appShoppingCart = {
    awaitTimes: {
        s:      2000,
        m:      5000,
        l:      10000,
        xl:     30000,
        xxl:    60000,
        xxxl:   120000,
    }
}

export const commonSelectors = {
    elementByDataTest:      (data) => `[data-test="${data}"]`,
}