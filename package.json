{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "testHTMLReport": "npx cypress run --reporter cypress-mochawesome-reporter",
    "testSpecReport": "npx cypress run --reporter mocha-teamcity-reporter"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^14.0.0",
    "cypress-mochawesome-reporter": "^3.8.2",
    "mocha-teamcity-reporter": "^4.2.0"
  }
}
